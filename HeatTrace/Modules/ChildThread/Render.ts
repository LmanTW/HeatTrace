// Render
class Render {
  // Render The Heatmap
  public static renderHeatmap (width: number, height: number, heatmap: Uint32Array): Uint8Array {
    
  }
}

// Layer
interface Layer {
  layer: number,

  data: Uint8Array
}
